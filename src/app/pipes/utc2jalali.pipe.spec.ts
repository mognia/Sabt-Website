import { Utc2jalaliPipe } from './utc2jalali.pipe';

describe('Utc2jalaliPipe', () => {
  it('create an instance', () => {
    const pipe = new Utc2jalaliPipe();
    expect(pipe).toBeTruthy();
  });
});
